# 依赖
node_modules/

# 微信开发者工具私有配置（含个人路径）
project.private.config.json

# 日志与临时文件
*.log
.DS_Store
Thumbs.db

# 云函数本地依赖
cloudfunctions/*/node_modules/

# 设计稿压缩包（文件夹版已入库，zip 为本机重复副本）
光影集 PhotoMuse 三版视觉 Mock.zip

# 部署密钥（本地保存，不入公开仓库）
.deploy-secrets.json
